.PHONY: install run build preview clean

install:
	npm install

run:
	npm run dev

build:
	npm run build

preview:
	npm run preview

clean:
	rmdir /s /q node_modules 2>nul & echo node_modules removed.
	rmdir /s /q dist 2>nul & echo dist removed.
